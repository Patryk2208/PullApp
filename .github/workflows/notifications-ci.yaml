name: CI — notifications

on:
  push:
    branches: [master, develop]
    paths:
      - 'src/services/notifications/**'
      - '.github/workflows/notifications-ci.yaml'
  pull_request:
    branches: [master, develop]
    paths:
      - 'src/services/notifications/**'
      - '.github/workflows/notifications-ci.yaml'

env:
  COMMIT_SHA: ${{ github.sha }}

jobs:
  build-and-test:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: src/services/notifications

    steps:
      - uses: actions/checkout@v4

      - name: Setup Go
        uses: actions/setup-go@v5
        with:
          go-version-file: src/services/notifications/go.mod
          cache-dependency-path: src/services/notifications/go.sum

      - name: Build
        run: go build ./...

      - name: Vet
        run: go vet ./...

      - name: Unit tests (race)
        run: go test -race ./...

  integration-test:
    runs-on: ubuntu-latest
    needs: build-and-test
    defaults:
      run:
        working-directory: src/services/notifications

    steps:
      - uses: actions/checkout@v4

      - name: Setup Go
        uses: actions/setup-go@v5
        with:
          go-version-file: src/services/notifications/go.mod
          cache-dependency-path: src/services/notifications/go.sum

      # ubuntu-latest runners ship with Docker, which testcontainers needs to
      # spin up Postgres + Kafka. Tests are gated behind the `integration` tag.
      - name: Integration tests
        run: go test -tags=integration -timeout 10m ./...

  dockerize:
    runs-on: ubuntu-latest
    needs: build-and-test
    defaults:
      run:
        working-directory: src/services/notifications

    steps:
      - uses: actions/checkout@v4

      - name: Docker build
        run: docker build -f Dockerfile -t pullapp/notifications:${{ env.COMMIT_SHA }} .