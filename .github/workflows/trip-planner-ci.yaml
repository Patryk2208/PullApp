name: CI — trip-planner

on:
  push:
    branches: [master]
    paths:
      - 'src/services/trip-planner/**'
      - '.github/workflows/trip-planner-ci.yaml'
  pull_request:
    paths:
      - 'src/services/trip-planner/**'
      - '.github/workflows/trip-planner-ci.yaml'

env:
  COMMIT_SHA: ${{ github.sha }}

jobs:
  build-and-test:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: src/services/trip-planner

    steps:
      - uses: actions/checkout@v4

      - name: Setup .NET
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: '10.0.x'

      - name: Cache NuGet
        uses: actions/cache@v4
        with:
          path: ~/.nuget/packages
          key: ${{ runner.os }}-nuget-trip-planner-${{ hashFiles('src/services/trip-planner/**/*.csproj') }}
          restore-keys: ${{ runner.os }}-nuget-trip-planner-

      - name: Restore
        run: dotnet restore

      - name: Build
        run: dotnet build --no-restore --configuration Release

      - name: Test
        run: |
          dotnet test \
            --no-build \
            --configuration Release \
            --logger "trx;LogFileName=unit-test-results.trx" \
            --collect:"XPlat Code Coverage"

      - name: Upload test results
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: trip-planner-test-results
          path: src/services/trip-planner/TestResults/*.trx

      - name: Upload coverage
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: trip-planner-coverage
          path: src/services/trip-planner/TestResults/**/coverage.cobertura.xml

  dockerize:
    runs-on: ubuntu-latest
    needs: build-and-test
    defaults:
      run:
        working-directory: src/services/trip-planner

    steps:
      - uses: actions/checkout@v4

      - name: Docker build
        run: docker build -f Dockerfile -t pullapp/trip-planner:${{ env.COMMIT_SHA }} .
