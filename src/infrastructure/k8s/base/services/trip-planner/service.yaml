apiVersion: v1
kind: Service
metadata:
  name: trip-planner
  labels:
    app: trip-planner
spec:
  selector:
    app: trip-planner
  ports:
  - port: 80
    targetPort: 8080
    name: http
    protocol: TCP
  type: ClusterIP