apiVersion: v1
kind: Service
metadata:
  name: driver-tracker
  labels:
    app: driver-tracker
spec:
  selector:
    app: driver-tracker
  ports:
  - port: 80
    targetPort: 8080
    name: http
    protocol: TCP
  type: ClusterIP